# NER Service Dependencies for macOS with Apple Silicon
# Python 3.12+ required

# FastAPI and web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart>=0.0.12

# NLP and NER processing with Apple Silicon support
spacy[apple]>=3.7.2
# Note: spaCy models need to be downloaded separately:
# python -m spacy download en_core_web_sm
# python -m spacy download en_core_web_lg

# Core dependencies with compatible versions for Apple Silicon
numpy>=2.0.0,<3.0.0
thinc>=8.3.0,<9.0.0

# Configuration and validation
pydantic>=2.9.2
pydantic-settings>=2.6.0

# HTTP client for async callbacks
httpx>=0.27.2

# Testing
pytest>=8.3.4
pytest-asyncio>=0.24.0

# Development and debugging
python-dotenv>=1.0.1

# Optional: Redis for caching (if enabled)
redis>=5.2.0

# Optional: Monitoring and metrics
prometheus-client>=0.21.1
